.vercel
.env*.local
.env.prod-local

# Service-account keys / credentials — never commit
secrets/
*.serviceaccount.json
.vision-usage.json

.sarvam-usage.json

# tesseract.js language data (downloaded at runtime)
*.traineddata
